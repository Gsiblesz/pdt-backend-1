// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
  createdAt DateTime @default(now())
}

model Registro {
  id        Int      @id @default(autoincrement())
  fecha     String   // Fecha del conjunto (puedes usar DateTime si prefieres)
  data      Json     // Objeto completo: incluye tiempos muertos, totales, máquinas, procesos, temperaturas, etc.
  createdAt DateTime @default(now())
  // Firma opcional para evitar duplicados exactos (fecha + id/nombre + tipo + start proceso1)
  signature String?  @unique
}
